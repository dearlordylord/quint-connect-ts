---
"@firfi/quint-connect": patch
---

Centralize trace-generation mode policy, fail fast on malformed `QUINT_BACKEND`, and share scoped subprocess lifecycle handling across the Quint CLI and Rust evaluator adapters.
