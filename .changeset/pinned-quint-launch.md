---
"@firfi/quint-connect": patch
---

Allow callers to pin the Quint executable with `quintBin` or `QUINT_BIN`, preserve the extendable `RunOptions` interface, and expose strict generation-mode option unions.
